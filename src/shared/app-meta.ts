import packageJson from "../../package.json";

/** Bumped by release-please via package.json (node release-type). */
export const APP_VERSION = packageJson.version;

export const APP_WEBSITE_URL = "https://celluloid.me";
export const APP_WEBSITE_LABEL = "celluloid.me";
